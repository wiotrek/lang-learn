import { SelectAnswersPipe } from './select-answers.pipe';

describe('SelectAnswersPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectAnswersPipe();
    expect(pipe).toBeTruthy();
  });
});
