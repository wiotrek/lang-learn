import { IdStrListPipe } from './id-str-list.pipe';

describe('IdStrListPipe', () => {
  it('create an instance', () => {
    const pipe = new IdStrListPipe();
    expect(pipe).toBeTruthy();
  });
});
